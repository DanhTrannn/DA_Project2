# Feature engineering scaffold

The SQL files in this directory define proposed Data Mining feature tables.
Models are disabled in `dbt_project.yml` and are not built by the current
staging-only pipeline.
