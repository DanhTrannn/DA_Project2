# Data Mart scaffold

The SQL files in this directory are design proposals for the next project
phase. Models are disabled in `dbt_project.yml` and are not built by the
current staging-only pipeline. Profit fields are estimates based on the
current product standard cost; they are not approved financial KPIs.
